<Style name="contours">

<!-- 12 zoom -->
	<Rule>
		&maxscale_zoom12;
		&minscale_zoom12;
		<Filter>([ele] % 500 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.5" smooth="0.5"/>
		<TextSymbolizer fontset-name="sans-book" size="8" fill="#bc7e55" upright="left" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>
	<Rule>
		&maxscale_zoom12;
		&minscale_zoom12;
		<Filter>([ele] % 500 != 0) and ([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.3"  smooth="0.5"/>
	</Rule>

	
<!-- 13 14 zoom -->
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom14;
		<Filter>([ele] % 250 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.5" smooth="0.5"/>
		<TextSymbolizer fontset-name="sans-book" size="8" fill="#bc7e55" upright="left" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>
	<Rule>
		&maxscale_zoom13;
		&minscale_zoom14;
		<Filter>([ele] % 250 != 0) and ([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.3"  smooth="0.5"/>
	</Rule>
	
	<!-- 15 zoom -->
	<Rule>
		&maxscale_zoom15;
		&minscale_zoom15;
		<Filter>([ele] % 100 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.5" smooth="0.5"/>
		<TextSymbolizer fontset-name="sans-book" size="8" fill="#bc7e55" upright="left" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>
	<Rule>
		&maxscale_zoom15;
		&minscale_zoom15;
		<Filter>([ele] % 100 != 0) and ([ele] % 20 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.3"  smooth="0.5"/>
	</Rule>
	
		<!-- 16-17 zoom -->
	<Rule>
		&maxscale_zoom16;
		&minscale_zoom17;
		<Filter>([ele] % 50 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.5" smooth="0.5"/>
		<TextSymbolizer fontset-name="sans-book" size="8" fill="#bc7e55" upright="left" placement="line" max-char-angle-delta="10" label-position-tolerance="100" minimum-path-length="200" spacing="200">[ele]</TextSymbolizer>
	</Rule>
	<Rule>
		&maxscale_zoom16;
		&minscale_zoom17;
		<Filter>([ele] % 50 != 0) and ([ele] % 10 = 0) and ([ele] != 0)</Filter>
		<LineSymbolizer stroke="#c45700" stroke-width="0.3"  smooth="0.5"/>
	</Rule>
	
</Style>
